const app = (function () {

  /**
   * cache DOM
   */

  /**
   * init
   */

  _init();

  /**
   * functions
   */

  function _init() {

  }

  function login(username, password) {
    console.log(username + " " + password);
  }

  return {
    login
  }

})();
